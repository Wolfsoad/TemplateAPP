@model IEnumerable<STRACT.Entities.HumanResources.UserHoliday>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataOfHoliday)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataOfHoliday)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserInTeamId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserHolidayId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserHolidayId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserHolidayId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
