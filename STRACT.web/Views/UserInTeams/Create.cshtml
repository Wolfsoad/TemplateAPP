@model STRACT.Entities.Users.UserInTeam

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UserInTeam</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Active" /> @Html.DisplayNameFor(model => model.Active)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="OrganizationalRole" class="control-label"></label>
                <select asp-for="OrganizationalRoleId" class ="form-control" asp-items="ViewBag.OrganizationalRoleId">
                    <option value="">-- Select Organizational Role --</option>
                </select>
                <span asp-validation-for="OrganizationalRoleId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ApplicationUser" class="control-label"></label>
                <select asp-for="ApplicationUserId" class ="form-control" asp-items="ViewBag.ApplicationUserId">
                    <option value="">-- Select Application User --</option>
                </select>
                <span asp-validation-for="ApplicationUserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
